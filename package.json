{
  "name": "branchreel-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root for the branchreel library and its playground demo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
